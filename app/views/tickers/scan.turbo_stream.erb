<%= turbo_stream.replace "indicator-grid" do %>
  <%= render(IndicatorGridComponent.new(
    signals: @signals,
    timeframes: ["15m", "1h", "2h", "4h", "24h", "48h", "1w", "1mo"],
    indicators: %w[rsi macd ttm_squeeze volume_spike ma_ribbon]
  )) %>
<% end %>
<%= turbo_stream.replace "tradingview-widget" do %>
  <div id="tradingview-widget-inner">
    <!-- TradingView widget will be embedded here by JS -->
  </div>
<% end %>
